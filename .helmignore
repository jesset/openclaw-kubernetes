# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.

.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/

# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~

# Various IDEs
.project
.idea/
*.tmproj
.vscode/

# Testing
test/
tests/
*_test.go

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml
scripts/

# Chart publishing artifacts
.chart-repo/

# Documentation (not needed in chart package)
docs/

# Local development files
local-values.yaml
values-local.yaml
*.local.yaml

# Helm dependencies (if using charts/ subfolder for deps)
# Uncomment if you have subcharts in development
# charts/*.tgz
# requirements.lock

# Environment files
.env
.envrc
